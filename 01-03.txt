String str = "Hello World";
    [  .substring ]
(시작인덱스, 끝인덱스+1) : 부분문자열 "추출"
    Good Morings
    01234 567891011

    [ .split(" ") ] 
: 
    분리문자열을 기준으로 문자열을 : "" ->문자열에 큰따옴표 안에 값을 기준으로 분리해서
				    배열에 넣는다.
문자열은 한글자식 다른 객체에 저장된다!!!!!
"Good" -> 객체 4개  -> .length

정렬
Ascending -> 오름차순
Desending -> 내림차순

함수
특정 기능 코드 모음
복잡한 코드가 반복되어 실행될때

반복되는것을 줄여준다.

함수 선언(Declaration)
함수 호출(Call)

프로그램의 간결성(가독성 개선)
복잡한 코드를 분리