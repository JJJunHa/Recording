배열(Array)
데이터타입 배열명[] = new 데이터타입[갯수];
데이터타입 배열명 = new 데이터타입[갯수];
데이터타입[] 배열명;

배열을 안하면 선언을 30번 해야함 !!!!
공간을 만드는 거다.

int[] arMath = new int[30];
int arMath[] = new int[30];

arMath[0] = 92; == int i =0;
                         arMath[i] = 92;
arMath[14] = 88;

arMath[14] = {96, 88, 100, 76, 78}; <-순차적으로 인덱스에 값을 넣을 수 있다.

long -> length -> 길이를 보관
wide -> width
high -> height

파일 찾을때 ctrl H 누르고 해당되는 키워드 찾으면 된다!!
콘솔에 에러가 뜬거를 다 지워야한다!!! 이상한 오류가뜸
